# FinTrust - Example environment (DO NOT use real secrets)
# Copy to .env and set real values only in local/dev; use Secrets Manager in production.

# Required in ALL environments (auth + API). Use the same value in both. No default; generate with: openssl rand -hex 32
JWT_SECRET=

# Auth service: login rate limit (max attempts per window; window in ms). Reduces brute force risk.
# LOGIN_RATE_LIMIT_MAX=10
# LOGIN_RATE_LIMIT_WINDOW_MS=900000

REACT_APP_API_URL=http://localhost:4000
REACT_APP_AUTH_URL=http://localhost:5000
AUTH_SERVICE_URL=http://auth:5000
NODE_ENV=development
